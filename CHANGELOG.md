# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 规范，版本语义遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Added

- **笔记上下文长度可设置**：此前「聊天要带多少正文给模型」是一个写死的常量 2000 字符，用户没有任何办法调整——长文档下模型只读到开头几页，却会自信地讨论整篇，而用户无计可施。现在它是设置面板里的一个字段（1000–32000 字符，默认 6000），聊天面板把它传给上下文构建器；窗口仍然按「开头 + 结尾」两段切分，所以调大它买到的是真实覆盖面，而不是更多同样的引言，被省略的中间部分依旧在上下文里注明。
- **插件对文档的写入纳入写入策略**：宿主交给插件的是真实 `NekoEditor`，其 `insertMarkdownAtCursor` 直接写模型——于是**什么权限都没声明**的插件反而拥有无条件的写入权，而诚实声明了 `ai` 的插件却拿不到任何 AI 能力。现在发布给插件的编辑器句柄被包了一层：只读接口（open/save/getView/建议相关/订阅）原样透传（格式化辅助工具本来就需要读文档），**写入**则走与 AI 自己插入完全相同的 `useAiPermissionStore().ask()`：「禁止写入」会拒绝插件写入，「每次询问」（默认）会弹窗并在用户同意后才落笔，「直接写入」不变。被拒绝时会报错并抛出（而不是静默），因此插件的写入失败会变成用户可见的提示，而不是一个「点了没反应」的按钮。
- **引用条目解析：坏一条不再毁掉整个库**（详见下方 Fixed）。
- **`scanTmpReferences` 汇报「完整与否」**：扫描结果新增 `complete` 标记，恢复流程以此决定是否允许执行不可逆操作。另：`VaultWalkResult` / `VaultFileIndex` 新增 `incomplete` / `isIncomplete`，目录列举失败不再等同于「这个目录里没有东西」。
- **一次创建，不覆盖（`create_note_with_free_name`）**：「从模板新建」与「新建每日笔记」此前都是「列出目录 → 选一个空闲名字 → 写入」。两步之间如果另一个实例（或同步工具）抢先建了同名文件，这次写入就会**直接盖掉**它。现在改用只创建不覆盖的后端路径（`create_new_file`，用 `hard_link` 发布暂存字节，「检查」与「创建」是同一个原子步骤），遇到「已存在」就按 `name-1.md`、`name-2.md` 的顺序换名重试（上限 5 次），全部被占用时给出可操作的提示，而不是退回覆盖写；浏览器演示（后端没有该命令）才沿用旧路径。
- **聊天单条消息的附件总字节上限（20 MB）**：条数上限之外，一条消息携带的图片总大小此前无上限——六张 10 MB 的图片就是 60 MB，在渲染进程里 base64 编码（约 80 MB 文本）后经一次 IPC 送出。现在 `MAX_ATTACHMENTS_PER_MESSAGE_BYTES` 在添加时按「当前累计量 + 这张」判断，超出只拒绝这一张并说明上限，已接收的仍然可以发送。
- **「跟随系统强调色」真的读系统色了**：此前它并不读系统，而是按明暗主题在代码里写死两种颜色——用户改了 Windows 强调色，应用毫无反应，而标签承诺的正好相反。现在由新的 Rust 命令读取 `HKCU\...\Explorer\Accent\AccentColorMenu`（Windows 设置里选的那个颜色），失败时回退到 DWM 的 `ColorizationColor`；不新增依赖（`reg.exe` + `CREATE_NO_WINDOW`，解析是纯函数所以单测不碰注册表），前端适配器放在 platform 层，store 依旧不 import Tauri。**调色板未改动**：系统色按 Lab（CIE76）距离映射到最接近的色板项，所以每个色板项都映射到自己。先试过的加权 HSL 启发式被否掉：它把 Windows 的鲜绿判成「lime」，把它的蓝判成「blue」，等于掷硬币。读不到时（非 Tauri、注册表失败）回退到原来的主题取色，并且设置面板如实说明是「读到了」还是「回退了」。
- **`create_new_file`：仅在文件名仍空闲时创建**：`ensureDailyNote` 是「先查后写」（stat → write），两个实例（或应用 + 同步工具）交错时后写者会**直接覆盖**中间出现的文件；对每日笔记来说就是毁掉另一处写入的内容。后端新增只创建不覆盖的写入路径（字节先落暂存，再用 `hard_link` 发布，`AlreadyExists` 直接失败），因此「检查」和「创建」是一个原子步骤，读者也不会看到写了一半的笔记；`AlreadyExists` 以标记形式返回，前端把「别人先建了」当作一个可处理的值，而不是要去解析的字符串。前端在浏览器演示（后端无此命令）时才回退到旧的写入。
- **插件回调隔离（toolbar / command）**：插件注册的工具栏按钮与命令此前直接由宿主调用，既不在激活的 try/catch 内，也不在 `emitLifecycle` 的隔离内——插件回调抛异常会从 DOM 事件处理器一路冒到控制台：没有插件名、没有用户提示，命令面板的收尾状态还会卡住。现在 `activatePlugin` 为每个按钮/命令注册一层隔离包装：捕获、记 `crash` 审计事件，并通过**生命周期错误已有的同一条通道**报给用户，来源写作 `toolbar:<id>` / `command:<id>`（用户据此知道是哪个按钮坏了）；同一回调每会话只提示一次，但每次都写日志。新增错误码 `PLUGIN_CALLBACK_ERROR`（「生命周期钩子抛错」不是按钮抛错该说的话）。
- **聊天附件条数上限**：一条消息里的图片此前只受单张 10 MiB 限制，张数不限——而它们会被 base64 编码进**同一个请求**，用户一张张添加即可把内存和请求体推到任意大。现在上限 `MAX_ATTACHMENTS_PER_MESSAGE = 6`，超出的部分被拒绝并提示上限（已接收的照常发送）。
- **聊天草稿按会话保存**：切到另一个会话再切回来，输入框里半截的问题和已附加的图片会被清空且无法找回。现在草稿按会话寄存：切走时存、切回时还原、删除会话时一并丢弃（含释放 object URL）、发送后遗忘。
- **`PersistencePort.set` 汇报是否写入成功**：端口不能抛（响应式 watcher 会跟着死），所以返回值是调用方唯一的知情渠道；`chatSession` 据此在超配额时退化为「只存文本」并提示用户，而不是静默报告成功。
- **AI 思考深度（`reasoning_effort`）**：设置面板可选「跟随服务商默认 / 不思考 / 极简 / 低 / 中 / 高 / 极高」，后端按服务商各自的形状下发——OpenAI 兼容端点用 `reasoning_effort`，Anthropic 用 `thinking.budget_tokens`（受 `max_tokens` 约束，额度不足时整段省略），Gemini 用 `thinkingConfig.thinkingBudget` 并与既有 `generationConfig` 合并。对着线上端点实测：服务端只接受这一档小写取值，`ultra`、`HIGH` 之类一律 400，因此未知取值在本地就被丢弃而不是转发。
- **AI 写入权限**：三档策略「每次询问（默认）/ 直接写入 / 禁止 AI 写入文档」。选择「每次询问」时会弹出提示，说明要做的是哪一类操作（插入到光标处 / 替换选中的内容）以及内容摘要，并提供「允许这一次」与「本次运行内始终允许」两个独立动作——按**操作种类**授权，所以允许插入不会顺带允许替换选区。策略持久化，会话授权只活在本次运行。「直接写入」不弹窗，「禁止写入」直接拒绝且不弹窗。未知策略（旧版遗留的存储值）一律回落到询问，绝不静默放行。
  - 两条写入路径都已接入：选区改写（润色/重写/翻译）在**发起请求之前**询问——被拒绝时不会把用户的文字发给服务商；聊天面板的「插入文档」在改动编辑器之前询问。
- **`services/tmpReferences.ts`**：按需扫描整个 vault（而不只是打开中的标签页）里引用的 `.tmp` 资源，供恢复闭环判断哪些暂存文件真的成了孤儿。
- **`e2e/task-checkbox.spec.ts`**：在真实渲染面板里点任务复选框，断言状态与落盘字节。

### Changed

- **只改大小写的重命名现在真的会生效**：上一轮虽然放行了 `note.md → Note.md`，但目标路径在移动前已被规范化成旧拼写，重命名「成功」而文件名没变（实测）。现在目标由请求的名字重建、经临时名两步完成，并把新拼写回报给调用方。
- **没有插件的笔记库不再弹插件提示**：列一次插件目录，没有就什么都不说——功能没被请求过，不是静默失败。

- **AI 的「思考深度」不再对空配置报错**：未配置 AI 时按 Tab 不再弹出 `aiGenFailed` 错误，而是安静地不动作。

- **插件能力的文档与实现对齐**：README 此前暗示可以安装自己的插件，而发行版因 CSP 门控**不会加载** vault 插件（本版本没有隔离机制）。现在在 persona、状态与插件开发三处如实写明，而不是让用户装完插件发现毫无反应。
- **`docs/PERF.md` 的说法与代码一致**：`index-build-10k` 此前只记录耗时、不设上限，而同页声称「每一项都有上界断言」。补上 `expect(ms).toBeLessThanOrEqual(2000)`（当前实测约 1.0 s），文档描述与测试代码终于说的是同一件事。
- **长 frontmatter 不再丢元数据**：笔记库扫描先把内容截到 400 字符、**再**去找 frontmatter 块，于是超过这个长度的块在其内部永远不闭合——`front` 返回空，笔记在列表、标签筛选、标签计数与搜索索引里都失去标题与标签，而「属性」面板（读完整块）仍然显示它们：同一篇笔记的两个界面互相矛盾。现在先定位块（有上限，避免「以 `---` 开头却永不闭合」的笔记拖垮扫描）再对**块之后**的正文取样，摘要也因此不再取自 YAML 文本。
- **删除/移动文件夹后笔记列表不再残留**：非笔记路径的 fs-change 此前只让文件列表失效并刷新附件计数；而文件夹的事件只会以**文件夹本身**到达（没有逐子项事件可依赖），于是其中的笔记会留在列表与索引里，点开时才发现读不到。现在影响已索引笔记的结构性变更会触发重新索引，并且判断依据是索引本身——普通的附件写入（每次粘贴都会发生）仍然不会重读整个 vault，这一点由第二个测试钉住。
- **`.tmp` 恢复判定改为「打开中的标签页 ∪ 全库扫描」**：此前只看打开中的标签页，于是「磁盘上有笔记引用了 `.tmp/xxx.png`、但该笔记的标签页没开」时，这张图会被当成崩溃残留——提示里的「恢复」会把它改名搬进 `attachments/<年月>/` 而笔记仍指向 `.tmp/…`（图片失效），超过 7 天后 `gc` 更会把它直接删掉。全库扫描是**惰性**的：`scan`/`gc` 先列 `.tmp`，目录为空（常态）就直接返回，不做任何逐笔记读取。扫描过期或失败时回落到「打开中标签页」集合——只会少判、多留垃圾，不会删掉仍在使用的文件。
- **打印输出改为「所见即所印」**：`print.css` 不再用 `display: block !important` 强行显示渲染面板（它会盖过 `v-show` 写下的内联 `display: none`）。源码模式下渲染面板的模型是被**刻意留旧**的，因此在源码模式切换笔记后打印会印出**另一篇笔记**。现在每个面板只打印自己真正持有的内容，源码模式打印完整的 Markdown 文本（CodeMirror 的打印支持按整篇文档度量，不会截断成视口内容）；对照模式只打印一份，避免同一篇内容印两遍。
- **命令面板在没有打开文档时不再列出格式命令**：这些命令需要编辑器才有处理函数，此前它们会被列出来并且点下去毫无反应（也没有任何提示）。现在直接说明「打开一个文档后即可使用这些命令」（新增专用 i18n 键 `palette.noDocument`，不再借用聊天面板的文案）。
- **插件授权判决按 vault 隔离**：此前只以插件 id 为键且从不清理，于是在 A vault 批准过一个危险插件后，B vault 里**另一个同 id 的插件**会静默获得授权、不再弹窗。现在与「完整性基线」使用同样的 `vault\0id` 复合键。拒绝则保持本次会话内有效（这是更安全的一侧），并且拒绝文案改为可执行的恢复方式（重启后再问 / 移除插件），不再指向一个并不存在的「插件设置」界面。
- **插件隔离（quarantine）现在真的可以解除**：钩子超时会被隔离，而拒绝文案让用户「重新批准并重载 vault」——但重置函数没有任何调用方，重载也清不掉标记，提示等于无法执行。现在加载 vault 时会重置隔离标记（只重置稳定性标记，所有授权闸门仍然照常执行）。
- **导出的 HTML 文件真正自包含**：图片此前写成 `asset://localhost/...`，文件离开应用（浏览器打开、拷到别的机器）后图片全是坏的——而同一个文件里的字体早已为了这个原因内联成 data URL。现在渲染器会告诉解析器目标形态：应用内打印/PDF 仍用显示 URL（输出逐字节不变），保存到磁盘的 HTML 请求内联为 data。SVG 附件会退化为 alt 文本而不是内联（`data:image/svg+xml` 可执行脚本，这是既有的安全姿态）。
- **组件体内的图片也会解析**：`![pic](…)` 写在 Callout/FloatBox 里时，此前因为「跳过组件体」的规则（那条规则对引用编号与标题 id 是正确的）而**不解析图片**，导出后是一个相对于保存位置解析的相对路径。现在该规则收窄到原本正确的范围。
- **参考文献列表补全转义**：纯文本分支把 `key`/`authors`/`year` 原样插值（同一行的 `title` 却转义了）。这些值来自用户自己的 `.bib`/`.ris`，而导出文件在应用之外打开（不受应用 CSP 保护），作者字段里的标记会变成活的标记。现与富文本分支一致地转义。
- **删除文件夹后可以找回**：文件树的垃圾桶按钮与右键菜单对目录同样可用，`delete_file` 也会把目录像文件一样移入回收站，但 `list_trash` 会跳过所有非文件条目——于是删除文件夹后「回收站」显示为空、没有计数、也没有任何找回入口，字节只能靠手工翻 `.nekowite-trash/` 找回。现在目录会被列出（`is_dir`）并能连内容一起还原。Rust 侧断言「目录不列出」的测试已改为断言真实契约。
- **回收站行显示真实文件名**：此前显示的是编码后的存储键（删除 `docs/a.md` 会显示 `docs%2Fa.md`）。另外，同一路径第二次删除会在键上追加时间戳，而 `decode_rel_path` 把时间戳并进了文件名，还原出来的是 `a.md-1757520000000`——这个扩展名应用不认，等于还原出一篇打不开的笔记。现在两者都由 Rust 侧解码（`display_name`、剥离碰撞后缀），「目标已占用」分支的重命名标记也插在扩展名之前。
- **还原到已删除的目录不再必然失败**：此前不会创建目标父目录，失败还会被显示成「请重试」——而重试永远不可能成功。现在会创建父目录（与 `rename_entry` 一致），仍然失败时错误信息会指出目标与实际原因。
- **修正 AI 权限策略标签的 i18n 键**：`describePolicy` 返回的是嵌套形状的键（`aiperm.policy.ask`），而语言包定义的是扁平键（`aiperm.policyAsk`），设置面板会渲染出一个缺失标签。由 console-clean 巡检（断言不请求缺失翻译）发现。

### Fixed

- **`[[wiki 链接]]` 不被当成链接，于是入链和图谱都看不到它**：链接提取器只匹配 `[文本](目标)`。wiki 链接在编辑器里能正常渲染成 chip、Ctrl+点击也能跳转，但**所有依赖索引的视图都对它视而不见**：入链列表显示「暂时没有笔记引用当前文档」，而同一目录下明明有一篇笔记引用了它；图谱里也没有对应的连线。（在打包版上用两篇笔记实测。）现在 `extractOutlinks` 同时匹配 `[[目标]]` 与 `[[目标|别名]]`（别名作为显示文本），并把无扩展名的目标规范成 `目标.md`——索引以解析后的 `<名字>.md` 路径为键，不规范化的目标会「存在但永远匹配不上」。`#片段` 会在补扩展名**之前**被拆出来再拼回结尾（否则 `[[目标#小节]]` 会变成 `目标#小节.md`，解析不到任何文件）；被转义的 `\[[...]]` 仍是普通文本（与编辑器解析器一致），外部目标照旧跳过。两种写法现在指向同一篇笔记，因此同一引用只有一条边。真机验收：链接面板的入链区块列出了引用它的笔记（修复前是空提示）。
- **「恢复历史版本」在屏幕上没有生效（随后一次按键就把恢复撤销）**：编辑器外部同步的幂等守卫比较的是「编辑器**打开时**的文本」（`appliedContent`），而不是「编辑器**现在持有**的文本」——用户此后的任何输入都替换了活动文档，并记录在 `lastLocalMarkdown`。于是「恢复到我刚编辑前的那一版」（最常见的用法）正好与打开时的文本相同，被守卫当成「已经应用过」而跳过：**文件回退了，屏幕没有回退**。用户以为恢复失败，接着一打字，编辑器就把被丢弃的那一版（连同新按键）写回标签并保存——刚刚丢掉的版本又悄悄回来了。现在守卫同时要求两个条件：文本既是最后一次应用的、且此后没有被编辑过（这两个字段只有在「没有输入」时才相等），原有的职责不变——真正的重复打开仍然被跳过，光标/撤销/滚动照旧不受 watcher 回声影响。真机验收 8/8（见下）。
- **一次保存的「一次性抑制」永不失效**：`suppressReapply` 在保存把 `tab.content` 与插件改写后的文本同步时布防，由该标签的内容监听消费，以免编辑器被重新加载（会重置光标）。这个布防没有期限：如果它等待的那次内容变化没有到来，它就一直挂着，直到**下一次真实的内容变化**（外部程序改写文件、冲突处理重新加载）被它吞掉——编辑器继续显示已经不在磁盘上的文本，下一次自动保存再把这份陈旧文本写回去覆盖更新的版本。现在布防带一个很短的生存期（它等的变化就在同一个 tick 内到达，2 秒足够），过期后既不再生效、也不会被报告为「已消费」。
- **`NoteListPanel` 重复声明并发常量**：它自己声明了 `CONTENT_SEARCH_CONCURRENCY = 8`，遮蔽了它同时传给 `searchWithIndex` 的那个导出常量，同一份调优值有两个副本、可能悄悄分叉。改为直接使用共享常量。
- **插件可绕过 AI 写入权限**（同 Added 条）。
- **笔记上下文长度写死**（同 Added 条）。
- **一条坏条目让整个引用库读成空的**：`parseRefs` 走的是 citation-js 的一次性整体解析，于是「两条正常 + 一条缺 key（或中途截断）」的 `.bib` 会解析出**零条**引用；侧栏同时显示「把 .bib / .ris / .json 放进 vault 根目录」的空状态，等于告诉用户文件根本不存在，而 store 静默接受了这个空列表。现在改为逐条解析：BibTeX 按花括号配平切块（保留 `@string` 宏），RIS 按 `ER  -` 记录切分，好条目照常载入，被跳过的条数由 store 明确告知用户。
- **任何 `.json` 都被当成 CSL 库**：vault 里的 `package.json` 被解析、注入一条无名条目并虚增徽标计数。现在只有「顶层数组且能产出至少一条可用条目」的 JSON 才算引用库。
- **不在库里的引用键和正常引用长得一模一样**：都在编号序列里显示成 `[3]`，作者和读者都无法分辨这个编号指向空处。editor-core 只拥有文档、不拥有引用库，现在由宿主注入键解析器：解析不到的键渲染为 `[?]`、不占用编号，并以醒目样式标出。
- **引用库只在打开 vault 时加载一次**：Zotero 的常规 workflow（应用开着时重新导出 .bib）会让库保持陈旧——徽标计数不变，新引用的键一直解析不到。现在 vault 的 fs 事件会（防抖地）重新加载引用库，并让已渲染的引用芯片同步刷新。
- **引用面板把「没有标题」说成「未在引用库中找到」**：这是两件不同的事实，现在分开显示。
- **崩溃恢复会在信息不完整时做不可逆操作（数据破坏）**：恢复流程要先判断哪些 `.tmp` 是孤儿，而它的两个动作都不可逆——「恢复」会把文件**移动**出 `.tmp`，GC 会直接删除。这个判断建立在一份可能悄悄不完整的扫描之上：读不出来的笔记（被别的程序占用、权限错误、目录列举失败）只是「没有引用」，而**不完整的遍历看起来和完整的一模一样**。于是只被那份读不出的笔记引用的 `.tmp` 被判为孤儿：用户被引导去恢复它（从而永久破坏那篇笔记的图片），或者 GC 直接把它删掉。现在扫描结果带 `complete` 标记，不完整时既**不给恢复提示**、也**拒绝执行 GC**；vault 切换导致扫描过期时按「不完整」上报，而不是退回到「只用打开中的标签页」（那是子集，不是替代品）。
- **新增笔记可能覆盖刚出现的同名文件**（同 Added 条，`createFromTemplate` 的换名重试）。
- **附件面板显示上一个 vault 的缩略图**（同 Added 条）。
- **删除笔记不再把它自己的图片文件夹留在磁盘上**：笔记粘贴的图片放在同级的 `<笔记名>_assets/` 目录里，而删除只移动了 `.md` 文件：那个目录会永远留在 vault 里——「附件」面板只遍历 vault 级的 `attachments/` 树，因此既列不出这些文件，也没有任何引用，占用的空间永远不会被回收。删除笔记看起来释放了图片，其实没有。现在两者作为一次操作进回收站，顺序是刻意的：先问目录是否存在（笔记删掉之后再问，「探测出错」与「本来就没有」无法区分），再删笔记，再删目录；目录失败会作为**部分结果**单独提示，而不是报「删除失败」——笔记确实已经删除（用户要的就是这个），谎报失败只会招来第二次操作和第二条回收站记录。两个删除入口都走这条路径；文件夹行**刻意不走**（文件夹删除本身就带走整棵子树，而把文件夹名当笔记名会误指向一个无关的 `<文件夹名>_assets` 兄弟目录——回收站按钮在每一行都有，这个守卫就是「回收空间」与「毁掉别人的文件」的差别）。
- **「从模板新建」会覆盖同名笔记**（同 Added 条）。
- **聊天单条消息的附件总量无上限**（同 Added 条）。
- **附件面板切换 vault 时会显示上一库的缩略图**：`resolveSrcs` 提交结果前不校验自己是否仍是最新一轮，迟到的 `resolveMediaPath` 会覆盖新库的缩略图映射表并清空「损坏」标记——面板列着新库的条目，却显示旧库解析出来的路径。现在与 `reload()` 用同一个 run 令牌守卫。
- **三个「死开关」**：`confirmBeforeDelete`（删除前确认）、`autoSyncScroll`（分屏滚动同步）、`renderTaskChecklist`（渲染任务列表勾选）此前只做了「定义 + 默认值 + 持久化 + 面板绑定」，**没有任何代码消费**——开关能点、能存，行为完全不变，等于对用户说了假话。现在：关闭「删除前确认」时文件树的删除按钮单击即删（一次明确的动作，不再追问），打开时保持取消/删除二次确认（默认仍为打开）；关闭「分屏滚动同步」时两个滚动 watcher 都被拦住，滚动一侧不再带动另一侧（进入对照模式、拖动分隔条时的一次性对齐**故意不受该开关影响**——那是布局，不是「跟随滚动」）；关闭「渲染任务列表勾选」时 `- [ ] 项` 渲染为纯文本（标记由 decoration 属性经 CSS 画出），点击不再切换状态，且该设置通过一次 meta-only 事务应用到现有编辑器上，撤销栈与光标位置都不受影响（不重建编辑器）。另外补上删除的 per-path 在途保护：双击不会再跑同一次删除（二次确认按钮此前也带着这个潜在问题）。
- **每篇从模板新建的笔记都继承了行尾空格**：默认日记模板以 `- `（标记 + 空格）结尾，于是每篇新日记落盘都带尾随空格，被各种 markdown linter 和保存时格式化工具标记。标记本身保留（那是留给用户输入的空中项目符号），空格去掉。同类问题另有 9 个模板文件的 13 处行尾空格（decision/experiment/literature/meeting/reading/research-plan/study/test-case），一并清理。
- **插件装饰被画了两遍**：`viewDecorations()` 会从**每一个**能找到的来源收集装饰——`someProp('decorations', …)` 先走视图顶层 prop，再走每个插件自己的 prop——而查找/拼写覆盖层的 provider 又把插件的装饰合并进了自己那一份，于是任何**非幂等**的装饰都会出现两次：AI 建议的 ghost 变成两个一模一样的 span，任务列表的标记亦然。内联标记掩盖了这个问题（同一个 span 画两遍看起来只画了一遍），所以它一直没被发现。现在 provider 只返回自己的查找/拼写标记。
- **长笔记只送开头给模型**：上下文块此前只截取笔记的前 2000 字符——写作长文的人（想按你正在写的部分提问）恰好永远看不到自己的正文，模型则对着开头自信地讨论一篇主题在五十页之后的笔记。现在预算被拆成「开头（这篇是什么）」与「结尾（工作是哪儿）」，省略提示插在缺失处；**选区**仍从开头截断（选区是用户指出的部分，砍掉后半段等于丢掉用户想展示的内容）。
- **AI 回答中途失败却被当作完整回答**：先流式输出了一段文字、随后请求断开时，结束路径与正常完成完全一致，于是半段话看起来是完整答复。现在这种回答会被标记为 `interrupted`（面板关闭中途时早已使用同一标记）。
- **`imageNotice` 从不渲染**：store 记录着「图片过大 / 因存储上限被移除」的解释，面板在转换时把它丢掉且模板从不渲染——附件就这么无声消失。现在随消息往返并在气泡下显示。
- **聊天历史可能整段消失**：持久化端口吞掉配额异常，`chatSession.persist` 的「去掉图片重试」分支永远不会执行——写入静默失败，下次启动整段对话（不只是图片）都没了。现在写入失败会重试、并在面板顶部显示警告；连纯文本都写不下时如实告知用户（此对话只在当前窗口）。
- **文件监听建立失败被静默吞掉**：`indexVault` 在 `try/catch` 里订阅 fs 变化事件，失败就 `listener = null` 继续跑——之后应用完全不知道外部新增/修改/删除了文件：笔记列表、附件徽标、内容索引都会停留在建索引那一刻，界面却毫无异样。现在失败会报告一次（日志带 vault），并且「重建索引」这一唯一的刷新入口会**重试订阅**（成功时告知用户已恢复），而不是只剩「切走再切回」这条没人猜得到的路。
- **应用内重命名会把已打开的标签页从文件上摘掉**：watcher 把重命名报告为**父目录**的变化，应用级外部变更服务于是逐一读取该目录下的标签页——而标签此刻仍指向旧名字（旧名字在重命名落地后就消失了，`renamePathInTabs` 还没跑）。读取失败 → 判定「在应用之外被移动或删除」→ 标签被分离成「未命名」、下一次 Ctrl+S 弹出原生另存为；脏标签更糟：文字还在，文件归属没了。现在移动操作会**声明（claim）**它即将移动的路径（是声明不是计时器，持续整个操作），外部变更服务把声明的路径留给正在进行的移动；目录的声明覆盖其内部笔记。另外补上「重命名已落地但正文改写失败」的修路：两种移动方式（内联重命名 / 拖拽）都会把标签重新指向真实存在的文件并让编辑器采纳其字节（脏标签保留用户文字）。
- **插件工具栏/命令回调抛异常不再污染宿主**（见上 Added 条）。
- **命令面板/工具栏的插件异常隔离后**，一次失败不再让整个点击流程半途中断。
- **文件树重命名未在磁盘操作前声明路径**导致上述分离（同一条修复）。
- **搜索覆盖层的延迟刷新在面板销毁后仍会执行**：`dispose()` 只停了计数播报，排队中的「空闲窗口 + 一帧」刷新仍会跑向一个已销毁的编辑器——在真实使用中表现为关闭时偶发报错，在测试里直接把整个 desktop 测试进程带崩（未处理的帧错误）。

- **作者写的 `<br />` 会被解析阶段删除**（静默数据损坏）：commonmark preset 会无条件删掉值为 `<br>`/`<br/>`/`<br />`/`<br >` 的 html 节点（那本意只是「空段落标记」这一个约定），于是 `line1<br />line2` 打开成 `line1line2`，下次保存就把丢换行的结果写回文件；表格单元格里更严重，因为 GFM 单元格里这是唯一能写换行的方式。
- **表格剪贴板劫持所有 Ctrl+V**：复制过一次单元格后，往任何表格里粘贴都会用旧缓冲区覆盖当前单元格；`preventDefault()` 还让原生 paste 根本不触发，往单元格粘图片彻底失效；缓冲区甚至能跨文档、跨编辑器实例存活。现在拦截要求「当前是单元格选区」且「缓冲区属于当前文档」。
- **光标在表格单元格里插入块级内容会把表格拆成两张**：分隔线、组件、表格对话框、插入图片、AI 插入、行间公式都会拆表并留下伪空行（GFM 单元格只接受段落，ProseMirror 只能把块「提升」出去）。现在这些在单元格里被拒绝（文档一字不变），行内内容照常插入。
- **在单元格里输入 `---` 会拆表**：preset 的分隔线输入规则没有 schema 守卫，第三个按键就拆表。现在换成带守卫的同 regex 规则。
- **单元格里 Shift+Enter 的硬换行保存后什么都不剩**：现在写成 `<br />`，与解析侧同一约定。
- **单元格里公式的 `|` 变成 `\|`**：LaTeX 里 `\|` 是双竖线，渲染结果与作者所写不同；现在有对应的反解。
- **公式里的 `$` 会把公式腰斩**：`a$b` 写成 `$a$b$` 会被读成公式 `a` 加文本 `b$`；含 `$` 的 latex 改用 `$$…$$` 包裹。
- **表格对话框行数下限不一致**：步进器允许 1、对话框 clamp 到 2，用户请求的尺寸被静默改写；现在共用同一组上下限。
- **切笔记后第一次 Ctrl+Z 被吞掉**：上一篇的历史还在栈上，其逆操作经位置映射后成为空操作，表现为「撤销坏了」。
- **代码块里粘贴富文本会拆开代码块**：现在代码块内只取纯文本。
- **引用编号每按键全量重算**（800 处引文实测约 96ms/按键）：每个文档版本只算一次。
- **渲染编辑器里 Tab 被 AI 补全吞掉**：焦点出不了编辑器，未配置 AI 时每次误按还弹错误；表格里按 Tab 换格还会同时触发补全。
- **输入法下 Enter/Esc 被抢走**：重命名对话框、文件树内联重命名、属性面板都不检查合成态——按 Enter 选词会直接提交（文件可能被改成半截拼音），按 Esc 想取消候选词会关掉整个对话框。现在统一走共享守卫。
- **文件树无法用键盘操作**：文件名是 `<span>`（Enter 无效），唯一的 Tab 停靠点是看不见的删除按钮。现在文件名是按钮、caret 有可访问名与 `aria-expanded`。
- **一次 Esc 会关掉所有打开的对话框**：它们都监听 `window`，而 `stopPropagation` 取消不了同一 target 上的其它监听器。现在有模态栈仲裁。
- **冲突提示与 AI 写入授权把焦点放在危险按钮上**：回车即丢弃本地改动 / 即批准写入。现在焦点落在对话框本身。
- **模板选择器打开后不接管焦点**：第一次按 Esc 什么都不会发生。
- **设置面板与命令面板声明 `aria-modal` 却没有焦点陷阱**：Tab 会走进被遮住的界面。
- **插件授权/完整性对话框不符合整套对话框约定**：没有 role、没有焦点陷阱、Esc 也不关。
- **大纲与历史面板在列表有内容时仍显示「没有内容」提示**。
- **AI 设置区四个标签硬编码英文**：对应翻译早已存在却零引用。
- **冲突提示的英文句式把路径占位符置空**，读作 `Disk content of  changed`。
- **`suppressReapply` 是模块级布尔**：A 标签的后台保存会 arm 它，而被消费的往往是切标签那次内容变化——编辑器模型里还是上一篇的文字，下一次按键就把它写进当前笔记。
- **收藏/最近跨 vault 被剪枝删除**：只有一份存储，剪枝谓词却是「当前 vault 的索引里有吗」。现在按 vault 分桶，旧格式会迁移。
- **documentList / view / bootstrap 裸用 localStorage**：getter 抛错的 webview 里 store 构造即抛（白屏），配额满时点收藏会抛到事件处理器。
- **窗口几何物理/逻辑像素混用**：125%/150% 缩放下保存的尺寸下次启动被裁成整屏并贴左上角，副屏窗口被拉回主屏。
- **拼写建议弹窗坐标过期**：文档变化后点击建议会改错位置，文档变短还会抛 `RangeError`。
- **`savedContent` 存的是序列化结果而非磁盘字节**：CRLF 文件上任何外部触碰都被当成真实修改（干净标签被 reload 丢光标，脏标签弹不存在的冲突）。
- **附件角标可能写入旧 vault 的计数**。
- **源码面板撤销历史是「每编辑器」而非「每文档」**：切笔记后第一次 Ctrl+Z 会把上一篇删掉的文字插进当前笔记，并可能被自动保存落盘。

- **重命名任意文件都会破坏回收站**（P0，静默数据损坏）：`move_trash_key` 用 `name.strip_prefix(&from_key).unwrap_or("")` 取后缀，再判「后缀为空即精确匹配」。`strip_prefix` 在不匹配时返回 `None`，`unwrap_or("")` 把「与我无关」变成了「就是我」——回收站里**每一条**都会在重命名别的文件时被改名成新 key，两条落在同一毫秒时 `fs::rename`（Windows 上覆盖已有目标）会直接销毁其中一条。实测：回收站放 a/b/c 三条，重命名 `keep.md` 后只剩两条同名的 `keep2.md*`，`CONTENT-b.md` 的内容永久消失；被改名的条目「还原」还会恢复到错误路径。现在按**解码后的路径**匹配（顺带让 legacy `__` 编码的条目第一次能跟随重命名），碰撞改用递增的 13 位时间戳——既保持 `strip_collision_suffix` 能识别的时间戳形状，也绝不会落在已占用的名字上。
- **历史快照失败会连带让保存失败**：`write_file` 里的 `snapshot_history(...)?` 把可选部件当成前置条件——历史目录不可写（权限、磁盘满、被同名文件占位）时**已有笔记完全存不了盘**，而用户只看到一句裸 OS 文案。快照改为尽力而为：`write_file` 返回 `Ok(Some(warning))` 表示「正文已写盘，另一件事没成」，窗口把它当提示显示。同时给硬链接加了回退——FAT32/exFAT（U 盘、SD 卡）根本不支持硬链接，那种盘上「新建笔记能存、改已有笔记必失败」的分裂现象现在没有了。
- **`.tmp` 清扫器会删掉用户自己的 `.tmp` 文件**：判定用的是「名字以 `.tmp` 结尾」，于是笔记文件夹里的 `draft.tmp` 会被下一次保存**永久删除**——不进回收站，也没有历史快照，无法恢复。现在只回收本程序自己写的形状（`.<名字>.<纯数字 nonce>.tmp`）。
- **「关闭全部」既不保存也不提示**：`closeAll` 直接循环 `removeTab`，未保存的编辑与未命名文档一次点击全部消失。现在与关闭单个标签同一套规则：先 flush 有路径的，再对未命名脏文档弹「保存/丢弃」，保存失败即中止；vault 切换改走非交互的 `removeAllTabs`（它已经 flush 并提示过，再问一次是另一个 bug）。
- **外部重命名文件夹后，标签页里的未保存文字被标成「已保存」**：`detachMissingPath` 无条件 `savedContent = content; dirty = false`，于是以 `dirty` 为准的每一道保护（关闭标签、切 vault、关窗口、自动保存）全部跳过它，而提示语恰恰写着「内容仍在这里」。现在 detach 后**仍有文字**的标签保持脏（空标签保持干净，免得对着白纸弹窗），自然走「另存为」。
- **源码模式跨文档撤销会把上一篇笔记删掉的文字写进当前笔记**：`addToHistory.of(false)` 只让整篇替换不进历史，CodeMirror 并不清空 undo 栈，旧文档的删除类事件经位置映射后仍可执行。实测「在 A 里删掉 `note ` → 切到 B → Ctrl+Z」得到 `BBB note B bodynote `，而源码面板 50ms 的 debounce 会把它发布给 `tab.content` 并自动落盘。现在 `history()` 放进 `Compartment`，换文档时先移除再重新加入。
- **事件合并丢掉了突发里的最后一次修改**：事件过滤器是前沿触发——窗口内第一笔发出、其余丢弃且**不刷新时间戳**，于是相隔 50ms 的两次外部写入只报第一笔，打开中的笔记 reload 到已经过时的内容，之后不再有事件纠正，用户接着保存就覆盖了更新的文本。现在被丢弃的事件会被挂起，突发安静后按尾沿重新发出。
- **隐藏目录过滤作用在绝对路径上**：vault 自己位于点开头目录（`~/.notes`）时，每个事件的绝对路径都含隐藏段，于是**全部被丢弃**——应用以为在监听，实际外部改动永远不会出现。过滤改为相对于监听根。
- **watcher 出错被静默忽略**：notify 报错（队列溢出、句柄耗尽）意味着事件已经在丢，而界面毫无察觉。现在发出 `kind: "resync"`，外部改动检测收到后逐个复核打开中的标签页。
- **AI 流内错误帧被忽略**：服务商在 200 响应里发 `{"error":…}`（OpenAI 兼容）或 `{"type":"error",…}`（Anthropic）时，解析器返回 `None`——半截答案被当作完整回答接受，`ai-done` 照发，用户可以把它插入文档而不知道它并不完整。现在会报错并结束请求。
- **`data: [DONE]` 不结束循环**：答案其实已经完整，但循环只认 EOF；服务端若在 `[DONE]` 后保持连接，用户要等到 120s 读超时，然后一个**已经成功**的请求被报成失败、幽灵写手的落笔状态也可能一直不结束。现在 `[DONE]` 即结束。
- **`finish_reason` 从未被读取**：`length`（输出预算耗尽）与 `content_filter` 和正常完成无法区分。现在会明确报出原因与要改的设置，而不是把截断的片段当成完整答案。
- **取消 AI 请求不能立刻生效**：取消只是从集合里摘掉 id，而流循环只在两次读取之间查看它——推理模型沉默数十秒期间，被取消的请求仍占着连接、并发槽位并继续计费，连续取消几次就会撞上「AI 请求过多」。现在每个请求带一个取消令牌，读循环与它赛跑，取消立刻放下响应体；被取消的流不再 flush 尾部、不再发 `ai-done`。
- **保存 API Key、设置/输入主密码会冻结窗口**：这四个命令是同步的 `#[tauri::command]`，跑在主线程——19 MiB 的 Argon2id 派生加 Stronghold 落盘期间界面完全无响应。改为 `async`，与文件命令一致。
- **只改大小写的重命名永远失败**：`note.md → Note.md` 被拒，提示还写着「target already exists: Note.md」——用户请求的名字被告知已经存在。现在允许（Windows 需要经由临时名两步完成：实测直接 rename 会「成功」但磁盘上仍是原名），并且返回**新的拼写**给调用方——解析发生在移动之前，直接复用会拿到旧大小写，标签标题看起来像什么都没发生。
- **没有插件的笔记库每次启动都弹一句英文安全提示**：即使 vault 里根本没有 `plugins/` 目录也会显示（还会往每个 vault 写一条插件审计记录）。现在先列目录：没有插件就什么都不说——功能没被请求过，那不是静默失败。提示文案也接进了 i18n。

- **公式对话框第一次打开时没有可视化编辑器**：对话框只挂载「此刻就能用」的那个编辑器，而首次打开时 MathLive 的懒加载还没落地，于是用户写下的第一个公式拿到的是朴素的 `contenteditable` 输入框，而不是这个功能所宣称的可视化公式编辑器——同一个按钮，时而好用时而不好用。现在对话框先挂上一个能用的编辑器，等 MathLive 到达后**升级**为它并把已输入的内容带过去；取值发生在升级的**那一刻**而不是请求升级时，否则升级期间的按键会被升级前的旧值覆盖（这条顺序有测试钉住，写反了就会失败）。真机验证：首次打开就存在 `math-field`，升级后内容原样保留。
- **在应用外重命名笔记所在文件夹后，下次保存会静默重建旧路径**：标签页不知道自己指向的文件已经不在了，`Ctrl+S` 于是把整篇笔记写回那个已被改名的旧路径，目录被重新创建——用户手上出现**同一篇笔记的两个分叉副本**，而界面上没有任何提示，哪一份是真的要靠自己去猜。现在外部变动检测会对变动路径下**打开中的标签页**逐个探测存在性：命中的标签页转为「未命名」并保留全文（下次保存走另存为询问），文件不会被凭空写回旧路径，同时提示用户发生了什么。判定刻意**不按事件种类门控**——Windows 上重命名文件夹发来的是两个 `modified` 事件（实测），按 `removed` 判断永远不会命中；真机验证 6/6。
- **移动笔记后图片不再失效**：移动只做了 `rename_entry` 与标签页路径更新，而笔记里的图片引用是**相对笔记**的（图库插入的是 `../attachments/…`，粘贴的是 `a_assets/pic.png`）——于是拖到别的文件夹后这些引用全部指向空处，笔记自己的 `_assets` 目录也被落在原地。现在移动会：先读正文（读失败则整体中止，不做任何移动）→ 连同名 `_assets` 目录一起移动并跟随改名 → 移动笔记 → 重写相对引用（自有资源跟随改名、共享图库跟随层级变化）。只改相对 markdown 目标（协议链接、绝对路径、页内锚点、围栏代码一律不动），没有需要改写的引用时**完全不写文件**。打开中的标签页同步采用重写后的文本；未保存的标签页则对它施加同一套重写规则，而不是被覆盖。
- **面板关闭时不再丢失已生成的回答**（P1）：关闭右栏会销毁聊天面板，而卸载钩子只回收了附件预览 URL——请求继续跑（继续计费）并写进已销毁的组件，回答则要等到切换会话或重载才可能看见；用户看到的是「问题还在、回答永远不来」。现在关闭面板会同时经由面板自身的流句柄与**应用级注册表**取消请求（句柄在启动 Promise 落定前是 `null`，只做一边会漏），并把用户消息与**已生成的部分回答**一起持久化、标记为「已中断」；重新打开右栏即可看到部分回答与「已中断」标记。一个 token 都没收到的回答也**保留并标记**，而不是像「中止」那样丢弃——一个答案凭空消失的问题气泡比明说被截断更糟。
- **frontmatter 面板不再重写它不负责的 YAML**（P0，静默数据损坏）：此前只要点进「属性」面板的某个字段再点走，整块 frontmatter 就会被重新序列化——块序列（`aliases:` + 缩进 `- one`）塌成空字符串、流式序列（`cssclasses: [wide, dark]`）被改成字符串、嵌套映射（`meta:` + `nested: 1`）塌掉、非 ASCII 键（`标题: …`）被直接丢弃。因为随后标签页变为「未保存」并被自动保存，损坏会在没有任何提示的情况下落到磁盘（「移除标签」走的是同一条路径）。现在解析器按**原始文本分段**保留每个键（含其缩进与空行续行），面板只编辑它渲染的那五个标量字段，其余段落逐字节原样写回；键名识别支持 Unicode 字母，缩进行永远不会被误认为顶层键。真机验证：改动并保存后上面那块内容逐字节不变（7/7），且「未改动就不写入」的既有保证仍然成立。
- **Windows 上向已保存的笔记粘贴/导入图片必然失败**（P1）：`assetsDirForNote` 是唯一没迁移到共享路径助手的函数，Windows 原生路径下它返回**绝对路径**（`C:\…\notes\a_assets`），而后端拒绝非 vault 相对的附件目录——于是粘贴总是以「插入图片失败，请重试」结束，而重试永远不可能成功。同一根因还让首次保存时的搬迁把 `![pic](C:\…\pic.png)` 写进笔记，Markdown 从此只在那一台机器上可用。真机验证：目录现在是 `notes/img_assets`（相对），保存成功、文件确实落到笔记旁、并能正常解析显示（5/5）。
- **同一类路径问题的其余四处**：`relativePathFromNoteVault` / `vaultRelativeFromNoteVault` 把 vault 内的绝对目标原样放行（显示解析同样受影响）；`treeDrop` 的后代判断用 `/` 后缀比较，Windows 上可以把文件夹拖进它自己的子目录；附件库与 frontmatter 面板用 `/` 切分取文件名，没有任何 `/` 时返回整条路径。
- **聊天：超过 10 MiB 的图片让「发送」变成静默无操作**（P1）：超限在 `send()` 里一个无人接管的 `await` 中抛出，而调用方是 `void send()`——草稿还在、消息没发出、没有任何提示，按钮看上去还活着。现在过大文件在**添加时**就被拒绝并说明上限，`send()` 的任何失败都会提示而不是无声结束；会话存储已有的「图片因容量上限被移除」提示也接了出来，不再是不明不白的消失。
- **AI 的「停止」现在真的能停住正在运行的请求**：请求 id 此前由后端生成，于是窗口侧在**首个事件到达之前**没有任何可引用的 id——而推理模型要沉默数秒（实测先到约 27 个 `reasoning_content` 分片才出现第一个正文分片）。在那段时间里点「停止」什么也不会取消：被放弃的请求继续流淌，迟到的分片随后被**下一个请求**采纳，显示成那个问题的答案（问算术题，答出一篇 600 字散文）。现在请求 id 由前端在发起前选定并随请求下发，且每条流只接受自己的 id——一条流永远不可能写进另一条流。取消同时会把该流标记为已作废，而不只是卸载监听器，因此已经在途的事件也不会再落到编辑器上。
- **渲染视图的任务复选框可以点击**：复选框是用伪元素画的，背后没有 DOM 节点，点击落在列表项的左侧内边距上、什么也不会发生——想勾掉一个任务只能切到源码模式把 `[ ]` 手改成 `[x]`。现在复选框有真实的点击目标：一次事务（单步撤销）、拒绝带修饰键的点击（Shift/Ctrl 点击仍是选择手势）、只在左侧内边距内生效、点正文不会误触。
- **链接解析的后备匹配在 Windows 上完全失效**：它拿原生反斜杠路径去和 `/` 前缀的候选比较，因此需要后备匹配的链接在该平台一律解析不出结果。现在改为按 vault 相对路径比较。
- **Windows 上 `\\?\` 前缀导致 vault 路径比较永远不相等**：`list_dir`（以及 `search_notes`、`resolve_media_path`、回收站列表、watcher 事件）把 `\\?\C:\...` 交给前端，而 vault 字符串来自文件夹对话框、没有该前缀。于是在 Windows 上：笔记列表/标签栏/文件树显示整条绝对路径而不是文件名；重命名是静默无效（`dirOf` 永远匹配不上父目录）；「在文件行新建文件」写进错误的目录；附件与拖拽目标解析失败。
- **外部改动检测不再依赖「文件夹」面板**：它此前完全挂在 `FileTree.vue` 里，而该组件只在「文件夹」面板显示时存在——默认的「笔记」面板下，vault 根本没有被监听（OS watcher 也是在这里挂的），外部编辑完全不被感知，下一次保存就会静默覆盖别人的修改。现在监听在 vault 提交处（`appBootstrap`）挂载，冲突判定与重载移入 `services/externalDocSync`（应用级），并删除了原先经由面板逐层转发的冲突事件链。
- **`fs-change` 事件类型词汇表**：类型此前是裸 `string`，于是索引协调器与图谱面板判断的是 `'remove'` 而后端发的是 `'removed'`——分支永不执行：被删除的笔记会一直留在缓存与持久化索引分片里，图谱也留着过期节点。类型现在是 `'created' | 'modified' | 'removed'`，写错字面量会变成编译错误。
- **同一次写入触发的事件风暴**：`notify` 会把一次逻辑写入报成多个事件（普通写入是两个相同的 `modified`；原子替换是 `removed` + `created`），每个订阅者都因此重复读盘/重复解析，替换还可能被误判为删除。现在同一路径上**相同种类**的事件会在短窗口内合并（不同种类、或窗口之外的同类事件照常转发，真实改动不会被吞掉）。
- 详见下方逐条提交记录。

### Added (earlier)

- **块级构造放置的回归网**（`blockPlacement.test.ts`，19 例）：把行间公式、表格、代码块、标题、列表、组件分别放进列表项、引用、脚注定义、表格单元格，断言标记词一个都不能少——这一批**没有发现新缺陷**，作为「静默截断」这一类问题的长期防线记录下来（真正的结论是「这里没问题」，不是「修好了什么」）。
- **导出嵌套语料**（`export/exportComposition.test.ts`，36 例）：把内容塞进表格单元格、脚注定义、列表项、引用及其两两嵌套（单元格里的链接/强调/行内代码/数学/高亮/双链/图片/引用/脚注引用/组件、引用里的列表/表格/任务列表/代码块/标题、列表项里的引用/代码块/表格、脚注里的列表/代码块/数学/链接、四层嵌套列表…），断言每个标记词都出现在导出结果里。上一行的表格管道缺陷就是这张网找到的。
- **源码模式字节保真契约（E2E `source-fidelity.spec.ts`，44 项）**：逐个断言「打开后源码面板显示的字节」与「未编辑直接 Ctrl+S 后磁盘上的字节」。其中 18 类写法（frontmatter、加粗链接、任务列表、脚注、标题里的 JSX、强调里的 JSX、数学、CJK/emoji、围栏代码、嵌套列表、引用、自动链接、图片、双链、高亮、引用文献…）**逐字节不变**；另外 9 类会被规范化，且每条都把**规范化后的确切字节**钉住，避免以后悄悄变化。
- **解析韧性 / 锚点一致性测试**：`mdx/parseResilience.test.ts` 把 JSX 放进每一种能容纳行内内容的容器（标题、强调、加粗、删除线、链接、高亮、列表项、引用、嵌套引用、表格单元格、脚注定义、整段组件），断言标记词一个都不能少、并且逐字节往返；`export/headingIds.test.ts` 新增「编辑器算出的 id ≡ 导出写入的 id」差分用例。
- **保存路径回归网**（`serializeFuzz.test.ts`、`editorRoundtripFuzz.test.ts`、`link/schema.test.ts`）：约 90 条文档语料（setext 标题、波浪线围栏、缩进代码、引用式链接、自动链接、HTML 块、对齐表格、脚注、引用、硬换行、实体、CRLF、转义、CJK/emoji/组合字符、超长行等）分别断言 `roundTrip` 幂等、编辑器 `open→save` 不丢节点、以及链接与文字 mark 的嵌套顺序；另有 3 条「有意的规范化」（引用式链接内联、空单元格写空行标记）被显式钉住，而不是当作失败被忽略。
- **10 个内置默认笔记模板**：每日日记、每周复盘、会议记录、学习笔记、读书笔记、实验记录、文献阅读、研究计划、测试用例与决策记录，覆盖记录、复盘、科研与测试工作流；模板选择器无需手动创建 `templates/` 目录即可使用，用户同名模板仍可覆盖内置版本。

- **15 套完整配色主题**：在默认/暖阳/森林/海洋/樱花/薄雾/石墨/午夜基础上新增薰衣草、沙漠、薄荷、咖啡、梅子、暮色、绯红；每套主题均带浅色/深色表面、文本、边框、语义色、代码高亮和阴影变量。
- **15 个强调色**：新增橙、粉、青、可可，与既有墨/珊瑚/蓝/绿/金/紫/石板/青/酸橙/玫瑰/琥珀组合，设置面板支持点击切换。
- **主题色卡预览**：设置面板从下拉框改为可视化卡片，只显示当前模式的单个默认色块，提升可选性与可发现性。

- **图片文件选择入口**：工具栏与命令面板的「图片」命令现在会打开原生多选图片选择器（图片扩展名过滤器），把所选文件复制进当前笔记的资源目录并插入 Markdown。新增后端命令 `pick_image_files` / `import_attachment`：字节在后端直接复制，不过 IPC 传 base64，保留磁盘上的原文件名，冲突自动加 `-<n>` 后缀；扩展名白名单 + 10 MB 上限 + 目标目录仍限定在 vault 内。
- **模式感知的插入服务**：新增 `services/editorInsert.ts`（按视图模式决定插入到渲染编辑器还是 CodeMirror）、`services/editorOwnership.ts`（判定当前由哪个面板负责文档文本）、`services/sourceCommands.ts`（内置命令的 Markdown 文本实现）与 `services/sourceView.ts`（源码面板把手柄注册给服务层）。
- **命令的 Markdown 等价注册表**：`registerMarkdownCommand(id, producer)` 让「插入富节点」的命令声明其 Markdown 形式，源码模式下改为插入文本（数学公式 → `$$…$$`、表格 → 3×3 GFM 表格、Callout/FloatBox → 对应 JSX）。
- **统一的命令分发入口** `services/runEditorCommand.ts`：工具栏、插件按钮与命令面板共用同一条模式感知分发路径。
- **模式感知的选区委托层** `services/editorTextSelection.ts`：读写「当前负责输入的面板」的选区，供 AI 改写等选区级操作使用。
- **控制台洁净度巡检** （`e2e/console-clean.spec.ts`）：在三种视图模式点遍全部工具栏按钮、走遍侧栏导航与右栏、按多组关键词执行命令面板、逐项遍历设置面板的全部配色与开关，任何 `console.error`/`warning` 或抛出都判为失败。这一层网住了单元测试看不到的渲染期问题。
- **分隔条拖拽回归测试**（`e2e/split-resize.spec.ts`、`LayoutResizeHandle` 单元测试）：覆盖比例型与像素型两种取值单位。
- **保存往返测试**（`e2e/save-roundtrip.spec.ts`）：Ctrl+S 后用测试夹具里的「磁盘快照」逐字节比对真实落盘内容，并重新打开验证；新增 `diskFiles()` 夹具读取内存 vault 的持久化结果。
- **i18n 覆盖测试**：`i18n.test.ts` 新增「源码里用到的每个 t() 字面量键都必须在两个语言里存在」的用例（原有的 zh↔en 对等测试无法发现两个语言同时缺失的键）。
- **统一的标题锚点 id**（`headingAnchorIds`）：标题深链的三个消费方（复制链接的锚点按钮、导出 HTML 的 `id`、跟随链接的滚动处理）现在共用同一份文档级 id 列表，同名标题按顺序获得 `-1`/`-2` 后缀。
- **导出链接安全策略**（`export/url.ts`）：链接/图片目标改为协议允许列表，`javascript:`、`data:text/html`、`vbscript:`、`file:`、`blob:` 一律不写入 `href`/`src`（链接文字保留）。图片额外允许 `asset:`（应用自身解析出的 vault 媒体 URL）与 `data:image/*`（仅光栅格式，SVG 排除——它作为文档打开时可以执行脚本）。控制字符混淆（`java\tscript:`）会被识破。
- **图片渲染测试**：`e2e/image-render.spec.ts`（4 项，夹具能把附件真正解码成 data URL，因此可断言 `naturalWidth`）；`image/nodeView.test.ts` 新增 5 项覆盖首帧、`load` 清错与 Retry 重解析。

### Changed

- **更正 Rust `storage::index_store` 的模块文档**：原文称「今天没有磁盘索引可存」，但前端的持久化全文索引就写在 vault 内的 `.nekowite/index/`（分片 JSON + `.nekowite/index.meta.json`，带校验和与原子替换）。现文档如实说明索引由前端持有、该 Rust 模块只是未来 Rust 侧索引的预留位置，并指出 `search_notes` 目前仍是实时遍历。
- **如实记录：打开笔记后保存会把 9 类写法规范化**（渲染模型是文档文本的权威来源，所以打开时标签页会采用模型的 Markdown 形式）。实测这 9 类**没有任何内容丢失**，只是字节форм改变，且「源码面板显示的内容」与「保存写入的内容」始终一致：
  - 行尾两个空格硬换行 → 反斜杠硬换行（`line one\`）
  - 连续多个空行 → 折叠为一个空行
  - 缩进代码块 → 围栏代码块（缩进与制表符保留）
  - `~~~` 围栏 → ` ``` ` 围栏
  - setext 标题（`Title` + `====`）→ ATX 标题（`# Title`）
  - 引用式链接 `[text][ref]` + 定义 → 内联链接（定义块被消费）
  - 文件末尾缺少换行 → 补一个换行
  - 空表格单元格 → 写入空段落标记 `<br />`，并把列宽补齐
  - 表格分隔行重新排版（`:--` → `:-`），对齐语义保留
  这些已作为**当前契约**写入 E2E 断言（含确切字节），而不是被当作失败忽略。「未编辑就完全不动原文件」是另一件更大的改动（需要在模型之外保留原文，并为局部修改定义规则），本次**未做**，如实记录在此。
- **如实记录一处未修复的上游行为**：milkdown 的 `visitEmptyLine` 会删除任何独立的 `<br>`（也包含与文字并列的那种），所以 `line one<br>line two` 在编辑器里保存后会变成 `line oneline two`。这是上游解析行为，本次未改动（导出侧只删块级标记，因此导出仍按既有策略转义显示这个 `<br>`）；已用取值探针确认，并把结论写进测试注释与开发记录，没有假装它不存在。
- **插件安全边界如实说明**：此前文档宣称「插件沙箱（webview/worker）」已实现，但插件实际运行在主窗口上下文。现文档（README/CHANGELOG）改为「沙箱隔离规划中」，并提供**权限声明机制**：插件可在 manifest/`definePlugin` 中声明 `permissions`（`ai`/`fs`/`network`/`clipboard`），声明敏感能力（`ai`/`fs`/`network`）的插件在激活前由宿主弹确认，未授权则不激活（纯 UI 插件自动放行）。
- **界面全面重制**（借鉴 Memoir 的设计语言）：自定义标题栏（红绿灯窗口控制 + 拖拽区 + 视图分段切换）、侧栏整合搜索/文件树/引用/回收站与主题切换、图标化格式工具栏（含标题级别下拉）、Memoir 风格内容排版（标题阶梯/任务框/代码块/表格/引用文献胶囊）、右栏文档信息（引用 + 历史版本）、CJK 感知字数与阅读时长状态栏、空文档欢迎态；窗口改为无边框自定义装饰。

### Fixed
- **回收站被应用自己的索引文件灌满**：搜索索引写入是「先落 `.nekowite/index/*.tmp` 暂存、提交后再删暂存」的原子写法，而删除走的是 `delete_file` 命令 —— 那条命令的职责是把文件**移进回收站**。于是每次索引重建都在用户的回收站里留下 `%2Enekowite%2Findex%2Fshard-*.json.tmp` 这类记录，实测一个刚建立的 vault 在首次索引后回收站就有 4 条，用户既看不懂也无法处理。现在 `.nekowite/`、`.nekowite-trash/` 下的路径（文件树本就对用户隐藏）直接永久删除，不再进回收站；`list_trash` 还会顺手清掉历史遗留的这类条目，已污染的 vault 会自动恢复干净。用户笔记的删除仍然照常进回收站、可恢复。
- **表格对话框在「有选中文字」时确认后什么都不做**：`表格 → 确定` 的处理里有一个「只允许折叠光标」的判断，所以先选中一段文字再插入表格时，对话框关闭、文档毫无变化，也没有任何提示。现在确认一律插入表格（并替换选中的文字），与光标折叠时、以及命令面板里的同一命令行为一致。
- **导出表格的每一行被包在各自的 `<tbody>` 里**：行组标签是在逐行循环里选的，于是 100 行的表格导出成 99 个 `<tbody>`，`tbody + tbody` 这类 CSS、复制粘贴和 DOM 工具都会看到 Markdown 里并不存在的分组。现在表头一个 `<thead>`、所有数据行共用一个 `<tbody>`。
- **工具栏「Table」按钮显示英文**：该按钮由 editor-core 注册（无 i18n），于是全中文工具栏里混进一个英文 tooltip。现在复用命令面板已有的 id→文案映射，内置按钮走中英文文案，第三方插件按钮仍用插件自己的文字；同时给注册按钮加上 `data-command-id`，E2E 不再依赖会随语言变化的 title。
- **远程图片显示「图片加载失败 / 重试」，而重试永远不可能成功**：生产 CSP 只允许 `img-src 'self' asset: data: blob:`，所以 `![](https://…)` 必然加载失败；原来的提示把这种情况和「本地图片暂时读不到」混为一谈，用户只能反复点重试。现在区分两者：本地失败仍是「图片加载失败 + 重试」，远程图片明确说明「远程图片未加载（受安全策略限制）」并提供「在浏览器中打开」。安全策略本身没有放宽。同时图片节点的提示文案改为可注入，应用在挂载编辑器时装入中英文本（此前这三条提示是 editor-core 里写死的英文）。
- **重试成功后失败遮罩可能不消失**：重试成功会换成新的显示 URL，但没有更新「当前期望的 src」，`load` 事件因此被当成过期事件丢弃，遮罩只能靠重试前的那次乐观清除。现在重试路径同样记录期望的 src。
- **导出标题锚点错位与重复**：mdx 组件正文里的标题会「偷走」一个文档标题的 id。导出时组件正文与文档共用同一份 id 列表（`headingIds`），但只有真正的文档标题才会被统计进去（编辑器里组件是原子，正文是不透明源码，其标题既没有锚点按钮也不在锚点列表里），于是正文里每有一个标题，后续文档标题的 id 就整体前移一位，最后一个 id 被重复使用 —— 复制的 `#slug` 深链因此指向错误的标题。实测：`# Aaa` / `# Bbb` / `<Box>`（正文 `## Inside`）/ `# Ccc` / `# Ddd` 会导出成 `aaa`/`bbb`/`ddd`/`ddd`；现在文档标题恢复为 `aaa`/`bbb`/`ccc`/`ddd`，组件正文里的标题不再带文档锚点（编辑器也没有对应锚点，给 id 只会产生永远不可达的链接目标）。
- **切换笔记后显示上一页的图片**：图片解析结果按原始 src 记忆，而解析器是在调用时按「当前笔记」把相对路径变成绝对显示 URL 的 —— 同一个 `pic.png` 在不同目录指向不同文件（图片存放于 `<basename>_assets/`），于是第二篇同名引用的笔记复用了第一篇的解析结果。现在解析缓存带上下文 token（vault + 当前笔记路径），token 变化即失效；同一笔记内仍然命中缓存，`resolveImageSrc(..., { refresh: true })` 的重试语义不变。

- **索引状态把「已删除/已重命名的笔记」判成最新**：`indexStateOf` 只检查「当前文件列表里的每个路径都有条目」，不检查反方向——「索引里有条目、但文件已经不在了」。删除或重命名会留下一条残留条目直到下一次重建；调用方若相信 `up-to-date` 就会跳过那次重建，于是搜索继续命中一个已经不存在的笔记，点开必然失败。现两个方向都算 `stale`。**如实说明**：该函数目前没有任何生产调用方（只有测试引用），所以这是一处潜在的契约缺陷而不是正在发生的故障——修它是为了让这个导出 API 的语义与文档一致，而不是宣称线上问题已解决。
- **表格单元格里的双链/组件会把整行拆开，重开后链接失效**：GFM 表格中 `|` 是单元格分隔符。普通文本节点由表格构造的 `|` 不安全模式自动转义，但**以原始 HTML 序列化的节点**（双链 `[[N|alias]]`、组件 `<Comp a={1} />`、空行标记 `<br />`）走的是 `mdast-util-to-markdown` 默认的 `html` handler——它把值原样输出。于是 `| [[N\|alias]] |` 存盘后成了 `| [[N | alias]] |`：多出一个单元格，重新打开时双链被拆成 `[[N` 与 `alias]]` 两段文字。现注册一个上下文感知的 `html` handler：当序列化栈位于 `tableCell` 内时，给**尚未转义**的 `|` 补反斜杠；已转义的保持不变（实测踩到的坑：对 `\|` 再转义会得到 `\\|`，反斜杠成了字面量、管道又变成分隔符，行照样被拆）。单元格外的原始 HTML 不受影响。
- **写在行内容器里的 MDX 组件会让整篇文档被静默截断（甚至存成空文件）**：`mdxComponent` 是块级原子节点，而 `mdxJsxMdast` 只在 `paragraph`/`listItem`/`tableCell`/`tableHeader` 中跳过「把 JSX 提升为块节点」的转换——`heading`、`emphasis`、`strong`、`delete`、`link`、`nekoHighlight` 都漏掉了。在这些容器里转换后，ProseMirror 构造父节点会抛 `createNodeInParserFail`，而 milkdown 把异常吞掉：`open()` 照常返回、内存里只剩解析到一半的文档，下一次保存就把这份残缺内容覆盖回用户的文件。实测 `alpha *em <Callout />* bravo`、`alpha **st <Callout />** bravo`、`alpha ~~dl <Callout />~~ bravo`、`alpha [lk <Callout />](url) bravo` 四种输入保存后是**空文件**；`# Title <Callout />` 会丢掉同一文件里其它所有块。现把上述六种容器并入「保留为原始 html」的集合：JSX 以行内源码形式留在原处，并逐字节写回。新增 17 条逐字节往返用例（其中 3 条正是此前会被清空的情形）。
- **标题里含公式/引用/HTML 时，复制的锚点链接在导出文件里失效**：导出计算标题 slug 时把原子节点的 `value` 也算作标题文字，而锚点按钮取的是 ProseMirror `Node.textContent`——它只拼接文本节点，原子（公式、引用、图像、原始 HTML、双链）一概不贡献文字。于是 `# Claim [@smith2020]` 在编辑器里复制出 `#claim`、导出里却是 `id="claim-smith2020"`；`# Formula $a^2$` 是 `#formula` 对 `formula-a2`。现导出只统计真正的文本节点，并补了一条**差分测试**：同一批标题上，编辑器锚点算出的 id 必须与导出 HTML 里的 id 完全一致（含重复标题的去重后缀）。
- **链接文字带格式时保存会把链接拆散**：milkdown 的序列化器按 `spec.priority` 决定同一段文字上多个 mark 的嵌套顺序（值越小越先打开，因而落在最外层），而所有文字 mark 默认同为 50，于是并列时沿用 ProseMirror 自身的顺序——其中 `strong` 排在 `link` 之前。结果是 `[**加粗** 链接](url)` 被保存成 `**[加粗](url)** [链接](url)`：强调被提到链接外面，链接本身还按文字段重复了一遍（`[*斜体* 链接]` 等同样）。现把 `link` 的优先级固定为 0（低于默认值），让链接始终包在文字 mark 外层；`inlineCode` 早已用 100 做镜像处理（必须最内层）。新增 9 个用例覆盖加粗/斜体/删除线/高亮/行内代码及其组合，改前 6 个失败。
- **导出把空行标记当成正文打印**：空段落会被 milkdown 序列化成独占一行的 `<br />`（这样有意的空行才能在重新打开后保留），解析时再由 `visitEmptyLine` 删除，因此编辑器里从不显示它。而导出直接解析磁盘原文、没有这一步，标记就以 `html` 节点进入渲染器并被转义成可见文字 `&lt;br /&gt;`——空表格单元格里最明显（单元格的空内容正是最常写标记的地方）。现导出侧做同样的删除，但**只删块级标记**：写在文字旁边的 `<br>` 是作者的内联 HTML，仍按既有策略转义显示。
- **导出丢失行间公式 `$$…$$`**：`remark-math` 产出的节点类型是 `math`，而渲染器只匹配了并不存在的 `displayMath`；叶节点落进返回空串的 `renderChildren`，于是**每一次** HTML/PDF 导出都会静默丢掉行间公式（行内公式不受影响）。现两种类型名都接受，并补了「同一份完整文档里没有任何节点消失」的回归用例。
- **导出与编辑器不一致的四类结构化内容**：导出渲染器只认识一部分节点，转换链也缺少编辑器扩展的识别步骤。其中两类会直接**丢失文字**：
  - `==高亮==`、`[[双链|别名]]` 在 remark 里本就是普通文本，编辑器靠 `highlightRemark`/`wikilinkRemark` 识别；导出链没挂这两个 pass，于是把 `==`/`[[ ]]` 原样印进导出文件。更糟的是 `nekoWikiLink` 没有渲染分支（引用节点无子节点），一旦挂上 pass 会**整段消失**。现导出链补上 `highlightMdast`/`wikilinkMdast`，并新增 `nekoHighlight` → `<mark class="nk-highlight">`、`nekoWikiLink` → `<span class="wikilink" data-target="…">别名</span>`（导出文件是独立文件，双链没有可跳转目标，故保留目标属性供下游恢复，而不是伪造一个坏链接）。
  - `- [x] 完成` 的勾选状态由 remark-gfm 记在 `listItem.checked` 上（`[x]` 字面量已被消费），渲染器无视该字段，任务列表导出后退化成普通列表。现输出 `<ul class="contains-task-list">` 与 `<input type="checkbox" disabled checked>`，并配套 CSS 去掉项目符号、对齐正文。
  - GFM 脚注的 `footnoteReference`/`footnoteDefinition` 没有渲染分支：引用标记是叶子节点，被 `renderChildren` 吞掉；定义则同普通段落排在文末，读者分不清哪句是注释。现按首次出现顺序统一编号，引用渲染为 `<sup class="footnote-ref">` 锚点，定义渲染为带编号与 `↩` 回链的 `<div class="footnote">`。
- **移除未使用的 `tauri-plugin-fs`**：该插件被初始化、`fs:default` 也被授权，但前端既没有 `@tauri-apps/plugin-fs` 依赖、也没有任何调用点（文件操作全部走自定义命令），属于纯粹的 IPC 攻击面。现移除插件初始化、`fs:default` 授权与 Cargo 依赖；随之生成的 ACL schema 减少约 7150 行（每个平台 3576 行）。
- **清理仓库中的运行期产物**：`daily/2026-09-08.md` 是应用在「把仓库根目录当作 vault」时自动生成的空日记（与已忽略的 `.nekowite/` 同类），在基线提交里被误纳入版本控制；现取消跟踪并加入 `.gitignore`（文件保留在磁盘上，未删除）。同时删除 `.oxlintrc.json`——内容是空的 `{"rules": {}}`，oxlint 既不是依赖也不在 CI 中运行，它的存在会让人以为有一道并不存在的 lint 关卡。
- **文档澄清**：`docs/dev.md` 中把「未命名 dirty 文档」等已实现项标记完成；并注明代码格式（prettier 配置）目前只是约定、CI 未做格式门禁（`prettier` 不在依赖与 lockfile 中）。
- **源码模式 Ctrl+Shift+Z 无法重做**：CodeMirror 自带的 `historyKeymap` 只在 `linux` 平台标记下绑定 `Ctrl-Shift-z`，Windows 上因此撤销可用、重做却毫无反应（`Ctrl+Y` 可用但不通用）。现显式绑定 `Ctrl+Shift+Z`（macOS 为 `Cmd+Shift+Z`），三平台一致。
- **源码模式下输入光标跳到文档开头**：源码模式时渲染面板仍在挂载（`v-show` 隐藏），其内容监听把源码的编辑经 Markdown 序列化器规范化后回写进标签页，替换了用户正在输入的原文，CodeMirror 全量替换文本导致光标回到位置 0——表现为「连按同一按键跳到开头」「源码模式按两下 Backspace 跳到开头」「回车跳到开头」。现源码模式下不再回写序列化结果，切回渲染模式时再按需重新同步；`persistMarkdown` 也不再在模型未变化时把序列化结果推入标签页。
- **可同时启动多个实例**：原先重复启动 NekoWite 会形成多个进程并共用同一知识库，第二轮实例恢复旧窗口时可能同时触发保存与文件重载。现已注册 Tauri 单实例插件：再次启动会阻止新进程，并将已有主窗口还原、显示并聚焦。
- **输入时光标乱跳**：应用自身写入、删除与恢复历史会在文件监听事件到达前标记为自写；文件监听对活动文档先读取磁盘并与已保存内容比较，一致时不再重载；编辑器对同一内容重复 `open()` 做幂等拦截，避免保存回环替换实时模型、清空撤销历史并重置光标与滚动位置。
- **关闭按钮可能失效**：右上角 X 现在先走正常的 `close()` 以保留“关闭前保存”逻辑；若原生关闭过程中出现异常，会自动调用 `destroy()` 作为最终兜底，确保程序不会因关闭流程异常而无法退出。
- **整个界面误被拖拽**：Tauri 原生 `data-tauri-drag-region` 在标题栏空白处可直接拖动窗口，但交互目标被冒泡后会导致侧栏、编辑区等整个界面出现疑似可拖动行为。现移除原生拖拽标记，改用显式 `startDragging()` 并将拖动范围严格限制在自定义标题栏空白区域；按钮、链接、输入框及可编辑元素不触发拖动，双击标题栏仍可最大化/还原，浏览器演示环境不触发窗口操作。
- **插件从不反激活**：切换 vault 只加载新插件、从不卸载旧插件，导致插件 A 的组件/命令/生命周期钩子泄漏进 vault B（且重复激活为静默 no-op，切回 A 时无法重载）。现切换 vault 时先卸载上一 vault 的全部插件再加载新插件。
- **vault 插件加载路径**：`services/plugins.ts` 此前用相对路径 `import(specifier)`，Vite 打包后无法解析成文件系统路径，产物中 vault 插件加载失败。现改为经 Tauri 读取插件源码后以 blob 模块动态加载（`/* @vite-ignore */` 阻断静态分析）；浏览器（memory）演示模式无真实插件文件，维持 mock 不加载。
- 工具栏 H1–H6/加粗/斜体/删除线/行内代码/列表/任务列表/引用/链接/图片/代码块/分隔线/MDX 插入命令此前全部未注册（按钮无效果），已在 editor-core 补全并可切换（toggle）
- 编辑器回写循环：编辑触发的全文档重解析（丢失撤销历史/浮动定位），现以本地序列化镜像识别回显；保存期间继续输入不再被旧内容覆盖
- YAML frontmatter 打开后保存即损坏（解析为分隔线+二级标题），现于 open/save 前后抽出与回拼，字节保真
- `open()` 进入撤销历史导致切文档后 Cmd+Z 清空新文档
- 序列化反转义过宽导致故意转义的 `\[文本](url)` 保存后变成真链接
- MDX 组件属性含 `<`/`>`/`{` 时保存后永久损坏；属性解析现支持引号内 `>`、无引号值与实体解码
- Tauri 2 命令参数 camelCase/snake_case 不匹配导致真实后端全部 IPC 调用失败；对话框阻塞主线程死锁（改为 async）
- 回收站：绝对路径删除后永远无法恢复（编码为绝对路径键）；路径编码改为可逆且无碰撞
- 历史快照写入非原子、恢复版本前不快照当前版本、崩溃恢复对"快照=磁盘"的情况误报
- AI：跨网络分块的 CJK 字符乱码（改为字节级 SSE 缓冲）、60s 总超时中断长输出（改为读超时）、重触发时旧流被采纳
- plugin-host：插件重复激活产生幽灵钩子/重复按钮；onSave 链式转换丢失中间结果；钩子清理重复执行；发射中注销跳过钩子
- **源码模式无法粘贴 / 拖入图片**：图片粘贴与拖放处理器此前只注册在渲染面板上，源码模式完全没有处理器，图片会以原始文本/HTML 落进文档。现处理器挂在两种视图共用的面板容器上（捕获阶段），源码模式下会先保存到笔记资源目录，再在 CodeMirror 光标处插入 Markdown。
- **插入图片按钮不弹文件选择器**：该命令原先直接插入一个 `src` 为空的图片节点——既没有选择文件，又在文档里留下坏图。现改为打开文件选择器；未注册宿主处理器的嵌入场景仍插入一个可见的占位节点。
- **源码模式下工具栏命令静默无效且抢走焦点**：这些命令是 ProseMirror 命令，源码模式下改的是隐藏的旧模型，用户看不到任何变化，焦点还被按钮抢走。现按视图模式路由：源码模式（以及对照模式下焦点位于源码面板时）执行等价的 Markdown 文本变换，执行后光标回到源码面板；工具栏按钮在 `mousedown` 上不再抢焦点。
- **源码模式下插入会覆盖原文（数据丢失）**：附件库、聊天「插入到文档」、引用插入此前都直接调用渲染编辑器的 `insertMarkdownAtCursor`；源码模式下会把序列化后的规范文本写回标签页，覆盖正在输入的原文。现统一走模式感知的插入服务。
- **对照模式下源码编辑被序列化结果覆盖**：渲染面板的 `persistMarkdown` 会把规范化 Markdown 写回标签页，源码面板随即整篇镜像过来并移动光标（「最后一个字符消失」类症状）。现按「标签页当前文本是否由源码面板产生」判定是否允许回写，并在回写前先冲刷源码面板尚在合并窗口内的编辑；对照模式的实时预览保持不变。
- **源码模式下 AI 改写/润色/翻译结果丢失**：`rewriteSelection` 从渲染模型读取选区、又把结果写回渲染模型；源码模式下该模型是隐藏且过期的，结果会在切回渲染模式重新打开模型时被静默丢弃。现改为通过模式感知的选区读写层操作实际负责输入的面板。
- **插件按钮（数学公式/表格/Callout/FloatBox）在源码模式下无效**：这些按钮原先直接调用各自的 `run()`，它们自行解析渲染视图，源码模式下插入落在隐藏模型里丢失。现统一走模式感知分发，并按注册的 Markdown 等价形式插入文本。
- **命令面板在源码模式下无声失效**：面板的每一项都直接调用 `getCommand(id)?.run()`，因此连「加粗」这类命令在源码模式下改的也是隐藏模型。现与工具栏共用同一分发入口。
- **`callout.insert` / `floatbox.insert` 在按 id 分发后失效**：这两个 id 只注册为工具栏项、没有命令注册，分发需回退到工具栏注册表查找。
- **对照模式下的分发依赖瞬时焦点**：命令面板打开时焦点移到其输入框，导致分发无法判断用户原本在哪个面板工作。现记住「最后聚焦的编辑面板」，命令从工具栏、面板或插件按钮发起时都能落到正确面板。
- **保存/导出/AI 上下文可能落后一次按键**：源码面板会合并按键后再写入标签页，而保存、导出与聊天上下文直接读取标签页文本。现 `saveTab`（覆盖显式保存、自动保存与关闭保存）、导出与聊天上下文在读取前先冲刷源码面板待提交的编辑。
- **源码模式下浮动框工具栏仍然可用**：该工具栏挂在共用面板容器上，源码模式下依旧显示，但其按钮编辑的是隐藏模型。现切到源码模式时清除浮动框选中状态。
- **对照模式分隔条拖不动（只能到固定位置）**：`LayoutResizeHandle` 把指针位移（像素）直接加到取值上，而分隔条的取值是 0.15–0.85 的比例，于是任何拖拽都会立刻撞到边界——表现为「只能拖到一个固定位置」。现新增 `deltaUnit`：比例型手柄按轨道宽度换算位移，侧栏/右栏这类像素型手柄行为不变。
- **命令面板的关闭状态残留**：`show()` 用两帧 rAF 延后淡入，而 `hide()` 从不取消它；快速关闭时那帧迟到的绘制会在 `hide()` 清空后重新把「已显示」置真，面板逻辑上已关闭却仍标记为在屏。现 `hide()`/卸载都会取消待执行的绘制帧。
- **命令面板按 Esc 在刚打开时无效**：淡入是延后两帧的，刚打开时「已显示」仍为假；若以它作为 Esc 的判断条件，打开后立刻按 Esc 不会关闭。现改为依据逻辑开启状态，并引入独立的 `closing` 状态：淡出过程中的 Ctrl+K 视为重新打开，而不是被进行中的关闭吞掉。
- **分隔条丢失调用方传入的 class**：组件模板有两个根节点（手柄 + Teleport 的拖拽参考线），Vue 无法自动继承属性，调用方的 `class="split-handle"` 被丢弃并告警，对应 CSS 规则形同虚设（打印时也未能隐藏分隔条）。现显式 `inheritAttrs: false` + `v-bind="$attrs"`。
- **首次保存未保存最后一次按键**：`saveTab` 的源码面板冲刷原先排在附件迁移之后，而迁移是整个文档的「读-改-写」，会基于过期快照改写文本并覆盖仍在合并窗口内的按键。现冲刷提前到读取 `t.content` 之前。
- **标签移除与 frontmatter 编辑会覆盖未提交的按键**：侧栏移除标签、frontmatter 面板写回都是整档读改写，此前未先冲刷源码面板待提交的编辑，会基于过期文本改写并回灌。现两者读取前先冲刷。
- **保存会丢失刚输入的内容（数据丢失）**：渲染面板的序列化是防抖的，而 `saveTab` 只冲刷了源码面板；在防抖窗口内按 Ctrl+S 会写入**上一版**文本，随后又把这份过期文本重新套回模型，直接丢掉刚敲的按键。现渲染面板也注册了可等待的冲刷入口，保存/导出/AI 上下文在读取文档前统一冲刷两个面板。
- **文件里被写入不换行空格 U+00A0**：在渲染面板于一段文字末尾输入空格时，浏览器插入的是 U+00A0，此前会原样序列化进文件——纯文本搜索/对比匹配不到、其他 Markdown 工具显示为异常字符。现 `editor-core` 在序列化与解析两侧都做归一化（源码面板不受影响），已含 U+00A0 的旧文件会在下次保存时被修正。
- **聊天面板会话相关的 4 个文案键缺失**（`chat.newSession`/`chat.sessions`/`chat.untitled`/`chat.deleteSession`），界面上直接显示原始键名。已补齐中英文，并新增覆盖测试防止同类问题再次逃逸。
- **导出 HTML/PDF 存在 `javascript:` 型 XSS**：链接与图片的 `href`/`src` 只做了 HTML 转义，而 `javascript:alert(1)` 不含任何 HTML 元字符，会被原样写入导出文件——分享出去或用浏览器打开即可点击执行。同一文件对原生 HTML 是故意转义的（注释写明 XSS-safe default），链接却无同等级防护。现改为协议允许列表（见 Added）。
- **图片解析失败被永久缓存**：失败结果也被 memoize（`.catch(() => src)` 把拒绝转成“成功”值），而应用侧解析器在没有 vault 时直接返回原 src，无法区分“vault 还没就绪”和“文件不存在”。于是首次解析“成功”地缓存了不可加载的相对路径，vault 就绪后也不再重试。现失败不入缓存，并新增 `invalidateImageResolution()`：vault 提交后由 `appBootstrap` 调用，已挂载的图片节点会重新解析。上一轮给 Retry 加 `refresh` 只是治标。
- **每个同名标题都复制同一个锚点**：编辑器标题锚点只用 `slugify(文本)`，所以三个「Same」标题的锚点复制的都是 `#same`，后两个链接永远落在第一个标题上。现改为让锚点解析自己真正拥有的 id（点击时按文档顺序计算），与导出、滚动处理共用 `headingAnchorIds`。
- **复制的标题链接里笔记路径出现两次**：链接构造是 `${vault}/${tab.path}`，而 `tab.path` 在部分流程里已经包含 vault 前缀（`list_dir` 返回的是解析后的完整路径），拼出来是 `vault/vault/note.md#slug`。新增共享助手 `notePathRelativeToVault` 先归一化再拼接。
- **导出后的标题深链是死链**：编辑器标题锚点复制的是 `#slug`，但导出 HTML 的标题不生成 `id`。现导出为每个标题生成与编辑器 `slugify` 一致的 `id`，同名标题按出现顺序加 `-1`/`-2` 去重。
- **应用内本地 Markdown 链接点击无反应**：`[文本](notes/other.md)` 此前不做任何处理。现按同 vault 路径解析并新开标签页；`#slug` 链接滚动到对应标题；其它协议一律 `preventDefault`，不再有让 webview 自行导航的余地。
- **后端 `save_attachment` 没有大小上限和扩展名白名单**：picker 用的 `import_attachment` 两者都有，但 base64 粘贴路径没有——前端的 10 MB 检查是唯一防线，插件/聊天等其它调用方可绕过，且可把文件重命名成 `.html` 落盘。现在后端同样强制 10 MB 上限（先按编码长度拒绝，避免解码超大 payload）并复用 picker 的图片扩展名白名单；前端重命名对话框也给出明确提示而不是等后端报错。
- **打开笔记时图片显示加载失败，点 Retry 才正常**：图片节点视图会先把文档里的相对路径（如 `welcome_assets/pic.png`）写进 `<img src>`，再等异步解析替换成 `asset://` URL。相对路径在应用源下必然 404，于是先抛 `error` 显示失败浮层；随后真正的 URL 加载成功，但节点视图**没有 `load` 监听**，浮层无人清除——只有 Retry 因为先重置了状态才看起来“修好了”。现在：解析器可用时不再先写入不可加载的原始路径（已可直接显示的 `http:`/`data:`/绝对路径仍立即显示，避免空白帧），并补上 `load` 监听在真正加载成功后清错。
- **Retry 可能永久无效**：解析失败的结果也会被 memoize，若首次解析发生在 vault 授权之前，之后每次 Retry 都只是重放同一个失败。现在 Retry 会带 `refresh` 重新解析；当解析结果与原路径相同（无更优 URL）时才附加 `?retry=` 时间戳强制浏览器重新请求。
- **Windows 下附件相对路径使用反斜杠**：`resolve_within_rel` 返回的是面向前端的 vault 相对路径（要拼进 Markdown 图片 URL、与文件树路径比较、编码进历史/回收站键），在 Windows 上却返回 `\`，导致插入的图片引用无法显示。现在该函数统一输出 `/`，同时修复了此前 3 个在该平台失败的附件测试。
- 其他：切 vault 不关闭旧标签导致保存失败、同文件重复标签页、删除文件后残留标签自动保存复活文件、关闭脏标签静默丢失内容、自动保存关闭后残留定时器、未命名文档 Ctrl+S 无响应、应用自身写入触发伪冲突弹窗、引用库文件（.bib 等）在真实后端不可见

## 验证与交付（第二十二轮，2026-09-13）

- 便携版：`release/nekowite_0.1.0_x64.exe`（未签名），SHA-256 `8513824839442ef0f1eaef5cc51441c3159cacb9da6829833a26ecb92b6ae36c`
- 真机验收：CDP 驱动**打包版本体**的 10 项探针（`apps/desktop/ai-lab/270-round22-acceptance.cjs`），**10/10**，控制台零错误：
  1. 笔记列表正常渲染，启动无报错；
  2. 应用内重命名笔记后**标签页跟随新名**（`note.md` → `renamed.md`），没有「在应用之外被移动或删除」的误报；
  3. 紧随其后的 Ctrl+S 写回**改名后的那个文件**，未产生第二份副本；
  4. 关闭「渲染任务列表勾选」后任务项为纯文本（`::before` 内容 `[ ]`，无 checkbox）；
  5. 关闭「删除前二次确认」后单击回收站按钮即删除（无确认行，文件确实从磁盘消失）；
  6. `system_accent_color` 在打包版返回本机真实系统色 `{r:0,g:120,b:212,source:"explorer-accent-menu"}`；
  7. 静止状态下没有重复的 AI ghost；
  8. 新建每日笔记的磁盘末字节为 `...09-13\n\n-`（无行尾空格）。
- 测试门禁：desktop 1388、editor-core 643、plugin-host 103、Rust 170、Playwright 137；`typecheck` / `lint` / `clippy -D warnings` 全绿。

## 验证与交付（第二轮，2026-09-13）

- 便携版：`release/nekowite_0.1.0_x64.exe`（未签名），SHA-256 `19e7a82bf1d91711f00a7f7197763d60e595f588813313ee92b996db9799f40b`（已被后续提交取代）
- 真机验收（CDP 驱动**打包版本体**，控制台零错误）：
  - `apps/desktop/ai-lab/270-round22-acceptance.cjs` **10/10**：笔记列表渲染、应用内重命名后标签跟随、随后 Ctrl+S 写回改名后的文件且不产生副本、任务列表纯文本渲染、关闭二次确认后单击即删、`system_accent_color` 返回本机真实系统色、无重复 ghost、每日笔记无行尾空格。
  - `apps/desktop/ai-lab/283-delete-assets.cjs` **6/6**：删除笔记后 `notes/doomed.md` **与** `notes/doomed_assets/` 都离开 vault 并出现在回收站（`notes%2Fdoomed.md`、`notes%2Fdoomed%5Fassets`），无关笔记不受影响。
  - `apps/desktop/ai-lab/285-vault-open.cjs`：三种 vault（无 .bib / 正常 .bib / 含一条坏条目的 .bib）都能正常打开；坏条目那一份的引用计数为 **1**（好条目照常载入），并弹出「refs.bib 中有 1 条记录无法解析，已跳过」。
- 真实 AI 端到端（`apps/desktop/ai-lab/286-live-ai.cjs`，对 `https://tokenflux.dev/v1` 的 `deepseek-flash` 实打实发请求，**6/6**，控制台零错误）：
  1. 普通补全流式返回（3 个分片 → `LIVE`）；
  2. `ai-done` 带完整正文，无错误帧；
  3. **思考深度开到「高」**：165 个推理增量，耗时 1863 ms；
  4. **思考深度设为「不思考」**：推理增量为 **0**（同题同模型）——设置面板上的这一档确实作用到了请求上；
  5. 故意用不存在的模型名：返回 403，并且**带上服务商自己的说明**（`The current group does not support the requested model ... Available models: deepseek-flash`），而不是一句无从下手的「请求失败」；
  6. 中途取消：`ai_cancel` 后 **3 ms** 内返回，不再等读超时。
- 测试门禁：desktop **1445**、editor-core **648**、plugin-host **103**、Rust **170**、Playwright **137**；`typecheck` / `lint` / `clippy -D warnings` 全绿。
- **最终便携版**：`release/nekowite_0.1.0_x64.exe`（未签名），SHA-256 `a379b3910742f945ff8f230fc336253a9f723ad0b37f274ace573825f2e5b048`。同一轮里真机探针对它复跑全部通过：`270-round22-acceptance.cjs` **10/10**、`283-delete-assets.cjs` **6/6**、`286-live-ai.cjs` **6/6**（对真实端点的发包，含思考深度开关的对照）、`240-editor-fixes.cjs` 前 5 项（第 6 项依赖 dev 构建的模块加载器，打包版无此能力）、`200-packaged-smoke.cjs` 6/8（后两项是探针自身的旧选择器问题）。
- **设置面板实机确认**（`apps/desktop/ai-lab/287-ai-settings.cjs`，打包版，AI 分区）：**思考深度**（不思考/极简/低/中/高/极高/跟随服务商默认）、**写入权限**（每次询问/直接写入/禁止）、**笔记上下文长度** 三项控件都在，且与前面 6/6 的真实发包结果相互印证（「高」确实产生推理增量，「不思考」确实为 0）。
- **首启路径**（`apps/desktop/ai-lab/293-first-run.cjs`，打包版，**4/4**）：没有记录过 vault 时显示欢迎页且不渲染主界面；空 vault 能打开且列表是「空的」而不是坏的（索引状态「已是最新」）；由 **Node 直接写入**（模拟别的程序建文件）的笔记在 **1 秒内**出现在列表里；点开后有可编辑面板、标签名正确。
- **界面面板实机复核**（`apps/desktop/ai-lab/294-ui-surfaces.cjs`，打包版，**6/6**）：对照模式两个面板都在；滚动同步**开**时另一侧跟随（0 → 3491），**关**时纹丝不动（0 → 0）——这一对正是本轮修好的那个「死开关」；命令面板能打开并列出 25 条；图谱面板渲染出「2 篇 · 1 条链接」；回收站面板列出刚删除的 `two.md`（含清空入口）。
- **回归清扫**（打包版，本轮修过的历史缺陷逐条复跑）：`210-backend-fixes.cjs` **7/7**——回收站里三条无关条目的路径与正文都没有被改写（P0 回收站破坏）、只改大小写的重命名成功并回报新名、用户自己的 `.tmp` 文件不会被顺带清掉、历史快照失败时保存仍成功并带出可读警告、无插件的 vault 不再弹无关提示；`240-editor-fixes.cjs` 前 5 项——`<br />` 不被吞、表格公式的单个竖线不被转义破坏、大小写重命名在目录项上生效。

## [1.0.0] - 2026-08-29

这是 NekoWite 的首个完整发布，涵盖 v1.0–v1.6 全部计划功能。

### Added

- **v1.0 核心体验**：应用壳（文件树/多标签/状态栏/设置）、工具栏 WYSIWYG 编辑、表格网格编辑、YAML frontmatter 保留、三态视图（对照分屏/单独源码/单独渲染）、MDX/JSX 组件节点、Markdown/MDX 往返保真、外部编辑热重载、用户插件骨架
- **v1.1 数学**：MathLive 可视化公式编辑 + 公式键盘 + `$…$` / `$$…$$` 往返
- **v1.2 引用**：`.bib/.ris/CSL` 导入、`@citekey` 侧栏引用、自动编号
- **v1.3 导出**：PDF + HTML 导出（含渲染后的数学与组件），KaTeX 字体内嵌为 data URI 的自包含导出
- **v1.4 AI**：AI ghost-writer，BYOK（OpenAI/Claude/Gemini/Grok/本地 LM Studio/Ollama），Tab 接收，加密密钥存储
- **v1.5 浮动元素**：浮动图片/文本框/贴纸，可任意摆放（自由画布辅助能力），导出时保留绝对定位
- **v1.6 深度插件**：生命周期钩子（onLoad/onActivate/onDocChange/onSave 等）、活动编辑器追踪、更全注册面、插件权限声明（敏感能力激活前确认；沙箱隔离（webview/worker）规划中）
